version: "3"

services:
  node:
    container_name: node
    image: node:${NODE_VERSION}-alpine
    ports:
      - 9000:9000
    volumes:
      - ./:/app:cached
    working_dir: /app
    environment:
      - NODE_ENV:${NODE_ENV}
    command: npm install
